export interface RawCipCheck {
  createdAt: string;
  exceptionDetails: string;
  exceptions: string[];
  socureReferenceId: string;
  status: string;
  submitForReview: boolean;
  updatedAt: string;
}
