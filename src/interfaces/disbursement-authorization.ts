export interface RawDisbursementAuthorization {
  authorizedAt: string;
  createdAt: string;
  lastOwnerVerificationRequestAt: string;
  ownerVerificationData: string;
  ownerVerificationType: string;
  ownerVerifiedAt: string;
  status: string;
}
