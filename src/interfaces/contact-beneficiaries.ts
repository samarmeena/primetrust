export interface RawContactBeneficiaries {
  allocationPercentage: string;
  beneficiaryType: string;
  contactId: string;
  createdAt: Date;
  dateOfBirth: Date;
  email: string;
  nameOfBeneficiary: string;
  relationshipToOwner: string;
  spouseConsentId: string;
  taxIDNumber: string;
  updatedAt: Date;
}
